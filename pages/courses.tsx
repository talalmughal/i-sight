import { DashboardLayout } from "@/components/layout";

export default function Courses() {
  return (
    <DashboardLayout activeTab="Courses">
      <div>
        <span className="text-white text-3xl font-bold">Courses</span>
      </div>
    </DashboardLayout>
  );
}
