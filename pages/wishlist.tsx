import { DashboardLayout } from "@/components/layout";

export default function Wishlist() {
  return (
    <DashboardLayout activeTab="Wishlist">
      <div>
        <span className="text-white text-3xl font-bold">Wishlist</span>
      </div>
    </DashboardLayout>
  );
}
